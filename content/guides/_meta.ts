import { MetaRecord } from "nextra";

const META: MetaRecord = {
  "jupyter-gpu": "Running Jupyter Notebook",
  "ollama-server": "Deploying Ollama Server",
  "vscode-gpu": "Running VS Code Server",
};

export default META;
